# Report Decomposition Handover Setup

This repository contains the report decomposition pipeline (`src/ct_rate`).

## Migration goals
- Keep code and required decomposition inputs/outputs.
- Rebuild environments from lock files.
- Avoid machine-specific paths.

See other handover docs in this directory for environment, data links, and smoke tests.
