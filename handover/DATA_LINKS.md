# Data and Outputs

Keep files required for decomposition workflow and model training merge flow:
- Input CSVs for decomposition runs
- Decomposition outputs consumed by training merge steps
- Combine scripts

Avoid copying heavy image volumes; use symlinks in downstream repos.
